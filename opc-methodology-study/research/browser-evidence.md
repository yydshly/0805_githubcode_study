# 浏览器验收记录

日期：2026-08-05
本地地址：`http://127.0.0.1:4173/`

## 已验证内容

- 桌面（1440×900）：首页总览、章节目录与阅读面板均正常显示；页面可视宽度 1425，与滚动宽度一致。
- 平板（768×1024）：目录和阅读区保持可读布局；可视宽度 753，与滚动宽度一致。
- 手机（390×844）：首屏和研读区收为单列，筛选标签可横向浏览但不显示干扰性的滚动条；可视宽度 375，与滚动宽度一致。
- 章节：点击“资产和被动收入”后，阅读标题、摘要、提问与展开分析正确切换；刷新后所选章节和“已理解”标记仍被恢复。
- 搜索：搜索无匹配项时显示 0 节和空状态；重置后恢复 21 节。
- 主题：已检查浅色与深色主题的可读性和层级。
- 本地笔记：输入个人笔记后刷新，页面显示“已从本地恢复”；验收结束后已清除测试笔记和学习标记。
- 健康检查：`site/app.js` 语法检查通过，本地服务器返回 HTTP 200，浏览器控制台没有 warning 或 error。

## 键盘复测说明

控件使用原生语义化按钮，且 Tab 焦点已抵达“展开分析”按钮。曾分别使用浏览器自动化的 Enter 键和内嵌浏览器的键盘注入尝试触发按钮，但该环境未可靠地产生按键激活。因此，建议在真实本地浏览器中用物理键盘再确认 Enter / Space 行为；这不会影响鼠标、触摸与已验证的焦点路径。
